$for(papers)$
$if(withold)$
$else$
\runsubsection{$title$}
\descript{$authors$}
$if(venue)$
$if(month)$
\location{$venue$|$month$}
$else$
\location{$venue$}
$endif$
$else$
$if(month)$
\location{$month$}
$else$
$endif$
$endif$
$descr$
\sectionsep
$endif$
$endfor$
